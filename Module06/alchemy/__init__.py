from .elements import create_fire, create_water

__version__ = "1.0.0"
__author__ = "Master Pythonicus"

# This just to silent the flake8 (unused importing),
# since the functions is just return so its okay.
create_fire()
create_water()
